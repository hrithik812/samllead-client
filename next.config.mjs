/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cms.shanta-aml.com','www.shanta-aml.com'], // Add the hostname here
      },
};

export default nextConfig;
